/** Persisted demo dataset (drivers, documents, overrides). */
export const BOF_DEMO_DATA_STORAGE_KEY = "bof-demo-data-v3";

/** Legacy key — hydrated once, migrated into v3, then removed. */
export const BOF_DEMO_DATA_LEGACY_STORAGE_KEY = "bof-demo-data-v2";

/** Older legacy key — should be ignored completely. */
export const BOF_DEMO_DATA_V1_KEY = "bof-demo-data-v1";
