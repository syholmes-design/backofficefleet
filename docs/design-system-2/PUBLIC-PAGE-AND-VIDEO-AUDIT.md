# Public Page And Video Audit

Status: Wave 0 audit, no public pages modified.

## Public Route Inventory

The current clean baseline contains 45 directory routes with `index.html`.

Key route groups:

- Core public pages: `/`, `/drivers/`, `/dispatch/`, `/safety/`, `/settlements/`, `/business-operations/`, `/policies-procedures/`, `/trust-governance/`
- Operational proof pages: `/operations-record/`, `/documents/`, `/document-readiness-engine/`, `/bof-vault/`, `/command-center/`
- Audience pages: `/private-fleets/`, `/government/`, `/carriers/`, `/sectors/`, `/aggregator-command-center/`
- Older demo paths: `/interactive-demo/`, `/animated-demo/`, `/demo/`, `/demo-paths/`, `/scenario-walkthrough/`
- Priority Fleet pages: `/founding-fleet/`, `/founding-fleets/`, and nested Priority Fleet routes

## Video Inventory

Detected embedded video:

- `Website/drivers/index.html`
  - Poster: `/assets/images/video-posters/bof-driver-vault.jpg`
  - Source: `/assets/videos/bof-driver-vault.mp4`

Detected supporting video-asset screenshots:

- `Website/video-assets/settlements/screenshots/`

## Current Content Risks

- Several pages still include repeated assessment CTA bands.
- Some pages use old Priority/Fleet Assessment language in multiple places.
- Older interactive-demo and animated-demo paths remain live alongside newer record-first pages.
- Some pages still route users to broad section headers instead of issue-specific records.
- Some pages use explanatory story cards where record-first proof would be stronger.

## Wave 0 Decision

No public page rewrite was performed in this pass. The DS2 work creates the foundation and identifies page-level conversion work for Wave 1.

## Audit Follow-Up

Wave 1 should begin with the pages where brand and navigation consistency affect most visitors:

1. Homepage
2. Command Center
3. Drivers
4. Dispatch
5. Safety
6. Settlements
7. Operations Record
8. Documents and BOF Vault
9. Policies & Procedures
10. Business Operations
