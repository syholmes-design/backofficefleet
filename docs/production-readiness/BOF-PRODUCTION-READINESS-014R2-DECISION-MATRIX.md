# Prompt 014-R2 — Decision Matrix

**Program:** BOF-ORC-P009-014-MASTER-V1.0  
**Assignment:** Prompt 014-R2 decision gate (not certification)  
**Predecessor:** Prompt 014-R `f7d755f59308122b71049e55e45a07671b027f54`  
**Rule:** One primary classification per row. Resolved is not claimed.

Classification codes:

- **A** ORCHESTRATOR REMEDIABLE  
- **B** OPERATOR / DEPLOYMENT ACTION REQUIRED  
- **C** PRODUCT CAPABILITY GAP  
- **D** LIVE DATA / INFRASTRUCTURE DEPENDENCY  
- **E** TEST INFRASTRUCTURE BLOCKER  
- **F** NOT ACTUALLY A BLOCKER  

| ID | Blocker | Primary Classification | Existing Capability | Code Change Required | External Action Required | Certification Impact | Evidence |
|----|---------|------------------------|---------------------|----------------------|--------------------------|----------------------|----------|
| B1 | Production AUTH_SECRET / DATABASE_URL unavailable on certification host | **B** OPERATOR / DEPLOYMENT ACTION REQUIRED | Yes. `auth.ts` `getConfiguredAuthSecret`; NextAuth only constructed when secret present. `lib/prisma.ts` fail-closed PostgreSQL `DATABASE_URL`. Names documented in `ENVIRONMENT_SETUP.md`. | No (fail-closed is correct; do not invent placeholders) | Operator supplies AUTH_SECRET and PostgreSQL DATABASE_URL on the actual certification/production host. Optional: link this worktree to the intended Vercel project and pull env (gitignored). This worktree has no `.vercel/project.json`. | Prompt 014 production configuration and authentication checks fail until present. Session 503 is fail-closed, not a completed login. | 014/014-R: `.env`/`.env.local` absent; process env unset; `GET /api/auth/session` 503 `AUTH_SECRET_REQUIRED`; `injected env (0) from .env.local`; `npx vercel env ls` unlinked |
| B2 | No real operator session / role | **B** OPERATOR / DEPLOYMENT ACTION REQUIRED | Yes. Credentials provider in `auth.ts` looks up `prisma.user` + `memberships`/`role`. `lib/authorization.ts` RoleCode includes DISPATCH, FLEET_*, BOF_OPERATIONS. Copilot requires those roles (`COPILOT_OPERATOR_ROLE_CODES`). `require-operator-session` gates generate APIs. | No (do not hard-code user or bypass auth). `provision:demo-user` is not a production-user factory for 014. | After B1: create/use a real User with passwordHash and ACTIVE FleetMembership with an operator role. Sign in through existing Credentials. | Authenticated Load File, mutations, and role matrix remain untestable. Scenarios A–J blocked. | `auth.ts` authorize requires DATABASE_URL + user.passwordHash; 014 `/loads/L001` session-gated; CC nav “session not established” |
| B3 | Payment remains UNSUPPORTED | **C** PRODUCT CAPABILITY GAP | Partial. Invoice *document* generate (`POST /api/generate/invoice`). Factoring HTML packets. Settlement hold *representation* on operating/payroll overlay. Prisma `Invoice` / `InvoicePayment` and `recordLoadPayment` exist in PI store. **No** `app/api` caller of `recordLoadPayment`. Operator settlement UI labels `payment: "UNSUPPORTED"`. | Not in this gate. Wiring `recordLoadPayment` to operators would be a product payment-path implementation, not a 014-R2 repair. 012/013 left payment UNSUPPORTED. | None that converts UNSUPPORTED → SUPPORTED. Host env does not create cash posting. | Prompt 014 Scenario G cash/factoring *payment* portion remains blocked under the unchanged 014 standard. Invoice/factoring documents are not cash received. | `lib/settlement/settlement-operating-display.ts` payment/invoicePayment UNSUPPORTED; SettlementDetailDrawer text; GAP-009-015; no API `recordLoadPayment(` |
| B4 | LIVE equipment PENDING LIVE DEPENDENCY | **D** LIVE DATA / INFRASTRUCTURE DEPENDENCY | Yes, as Prisma path: `Equipment` model; `EquipmentStatus` AVAILABLE/UNAVAILABLE/OUT_OF_SERVICE; `PATCH /api/dispatch/equipment/[equipmentId]/status` → `setEquipmentStatus` → `prisma.equipment.update`. DEMO T-102 OOS is DEMO_ONLY (`canonical-equipment-spine`, ADR-009-001). LIVE fields PENDING/UNKNOWN without rows. | No (do not fill LIVE from DEMO JSON; do not invent rows) | PostgreSQL DATABASE_URL + real Equipment rows for the fleet + operator session. Then PATCH status and verify dispatch/CC consumers. | Prompt 014 Scenario E cannot verify a genuine LIVE unavailable→available transition on this host. Observing DEMO T-102 OOS is not LIVE. | `lib/services/equipmentService.ts`; `buildCanonicalEquipmentSpine` pendingPrisma; GAP-009-028; validate-canonical-equipment-spine T-102 DEMO UNAVAILABLE |
| B5 | No durable safety restriction create/remove | **C** PRODUCT CAPABILITY GAP | Read/copy only. Workbook `Safety_Events.dispatchBlock` consumed as REFERENCE/operating copies (EVT-001/EVT-010). No Prisma Safety Event model. No `app/api` safety create/remove/release route. `OperatingException` is PI exception tracking, not a safety-restriction engine. `AccidentHistory`/`ViolationHistory` are DQF history records. | No in this gate. Connecting OperatingException as a safety restriction would be a new/wrong SOT. Do not build a safety engine. | None that creates the missing durable safety mutation. | Prompt 014 Scenario C cannot be executed without fabricating events. | Prisma `^model ` list has no Safety Event; API grep no safety write routes; GAP-009-010 workbook copies |
| B6 | Operator dataset remains DEMO JSON on this host | **C** PRODUCT CAPABILITY GAP (vs 014 production-path rule); DEMO shell is intentional under ADR-009-001 | Operator `(bof)/layout.tsx` always wraps `BofDemoDataShell` + `getBofData()`. Command Center route comment: Type DEMO. Canonical equipment/dispatch KPIs: BOF JSON + loaders; workbook REFERENCE; Prisma LIVE parallel and PENDING. Durable Prisma APIs exist but are not the CC/dispatch operator dataset. | No without a new product/ADR decision. Relabeling JSON as PRODUCTION is forbidden. | DATABASE_URL does **not** by itself switch CC/dispatch off DEMO JSON. | Prompt 014 “no DEMO → PRODUCTION” fails on this operator shell even after secrets, unless product authorizes a different production operator dataset. | `app/(bof)/layout.tsx`; `app/(bof)/command-center/page.tsx` Type DEMO; ADR-009-001; PI capability-matrix “Operator settlement UI remains DEMO DATA” |
| B7 | 390/768 browser validation incomplete | **E** TEST INFRASTRUCTURE BLOCKER | Product pages render; 014 measured 1366 overflow 0 on CC/dispatch. 013 previously measured 390/768/1366 overflow 0 on checked chain pages (GAP-009-023 VALIDATED). 014-R MCP dropped after Emulation 390. No 014-R2 evidence of a UI defect. | No UI change solely because the test stopped. | Re-run interactive browser at 390, 768, 1366 on a working browser tool/session during full Prompt 014. | 014 required those viewports in *that* audit. Incomplete 014-R evidence is not a product fail. Next 014 must actually test all three. | 014 cert §8; 014-R MCP “Server not found”; GAP-009-023 |

No row is marked resolved.
