# Prompt 016 — Prompt 014 Readiness Matrix

**Not a certification.** Does not replace Prompt 014. Does not declare production-ready.

**Branch:** `orchestrator/prompt-016-alignment`  
**HEAD at audit start:** `9185a3b4` (Prompt 015 closeout hash)  
**Host:** `npx next start -p 3010` after 016 rebuild of Prompt 015 code  
**Operator session:** `bof-operations@dev.local` / `BOF_OPERATIONS` / fleet `bof-service` (Cursor tab `af1381`)

Ready-for-014 means: a full Prompt 014 run is now a **legitimate certification attempt** on the current product, not that Prompt 014 will PASS.

| Scenario | Technical Path | Current State | Remaining Blocker | Scope Conflict | Ready for 014 |
|----------|----------------|---------------|-------------------|----------------|---------------|
| A | LIVE intake → assignment → dispatch → execution → proof → settlement **context** (not cash). `/loads` HTTP, `/api/dispatch/load/:id` lookup by id/reference/sourceRecordId, `/trip-release/:id`. | `/loads` **200**. Cookie-less and authenticated. LIVE load `86fd04a8-ce66-4153-9125-dccb054f7033` GET **200** (`PLANNED`, PROCESS INTELLIGENCE TEST). `/trip-release/86fd04a8-…` renders backend release (Not evaluated, no assignment). `/trip-release/L001` and `GET /api/dispatch/load/L001` remain **404 Load not found**. Operator intake `/dispatch/intake` remains DEMO packet workspace. | No Prisma `L001`. LIVE load used for the same workflow is unassigned / not evaluated. DEMO intake is not a production chain. | **CERTIFICATION SCOPE / ARCHITECTURE CONFLICT (C2):** 014 Scenario A as a **production operator** E2E still collides with ADR-009-001 DEMO shell. **LIVE DATA:** no L001 row. | **YES** — 014 can execute honestly: LIVE path exists; L001 absence is data, not a 500; DEMO intake is labeled by architecture. |
| B | Block present then resolved on the **same** authority used to block. | DEMO CC/Dispatch still show L001 HOLD / T-102 OOS (`DEMO_ONLY`). LIVE tractor `cmsw2tcep00022g5azbluiaeu` cycled UNAVAILABLE → AVAILABLE (016 restored AVAILABLE). LIVE settlement hold from 016 proof reject restored to `CREATED`. DEMO OOS was not cleared (would fabricate LIVE-from-DEMO). | Resolving DEMO T-102 OOS is not a LIVE mutation. LIVE PATCH does not rewrite the DEMO board. | **C2** — DEMO snapshot vs LIVE spine. Not a silent 015 reversal. | **YES** — 014 can distinguish DEMO block vs LIVE equipment/hold. |
| C | Durable Safety restriction create/remove consumed by Dispatch. | `/safety` remains REFERENCE/DEMO telematics. No Prisma Safety Event write API. No `app/api` safety create/remove. Workbook `Safety_Events.dispatchBlock` copies remain REFERENCE. | Durable create/remove does not exist. | **CERTIFICATION SCOPE CONFLICT (B2).** 014 Scenario C cannot PASS unless product or 014 changes. 016 did not implement Safety writes. | **YES** — 014 will FAIL C under unchanged 014; that is an expected scope conflict, not an unclosed 015 defect. |
| D | Durable driver eligibility INELIGIBLE → ELIGIBLE → persisted → downstream. | `/drivers` **200** (was 500). Authenticated `POST /api/dispatch/driver/cmtkkg28f0003to5ay7o9y72e/eligibility` INELIGIBLE **200**, then ELIGIBLE **200** (restored). Unauth POST **401**. `/drivers` roster still mixes DEMO `DRV-*` with LIVE id `cmtkkg28f0003to5ay7o9y72e`. LIVE spine panel does **not** list eligibility. | CC LIVE panel has no eligibility field. Roster is mixed DEMO/LIVE. | **C2 mixed surface** on `/drivers`. Eligibility API itself is LIVE. | **YES** — 014 can exercise the LIVE mutation; DEMO roster is not that mutation. |
| E | LIVE equipment AVAILABLE → UNAVAILABLE → downstream → AVAILABLE. Not DEMO T-102. | Authenticated PATCH on `cmsw2tcep00022g5azbluiaeu` UNAVAILABLE **200**, spine `eqMid: UNAVAILABLE`; restored AVAILABLE **200**, `eqEnd: AVAILABLE`. Dispatch/CC LIVE panels listed unit `ASSIGNMENT-TRACTOR-1786901161488-3` among 161 LIVE equipment. DEMO T-102 remains `DEMO_ONLY`. `/maintenance/cmsw2tcep00022g5azbluiaeu` **404**; DEMO maintenance still keyed as T-102. | DEMO assignment board still T-102. Maintenance HTML is not the LIVE id route. | **C2** if 014 requires DEMO Dispatch assignment chips to mirror LIVE PATCH. LIVE consumers (spine API + LIVE panels) **do** reflect LIVE status. | **YES** — 014 must use LIVE tractor + LIVE panel/API, not T-102. |
| F | Proof REJECTED → settlement HELD, persisted, consumed. | Authenticated `POST /api/dispatch/load/86fd04a8-…/proof/reject` **200**, settlement `e1ee3e8e-aa2f-4284-ba97-ad08861d8c8c` **HELD**, reason `016 alignment proof reject`. Spine holds visible during hold. Restored via Prisma: status `CREATED`, proof `RECEIVED` (`restoredProofs: 1`). Unauth POST **401**. | After restore, LIVE holds **0**. Workbook Settlement Holds (HOLD-001…) remain a **separate** REFERENCE/DEMO payroll snapshot. | Competing hold representations: Prisma LIVE vs workbook. Intentional split, not a failed 015 write. | **YES** — 014 can re-run the LIVE reject path on a real load id. |
| G | Settlement hold → **payment/factoring cash** → release. | `payment: "UNSUPPORTED"` in `lib/settlement/settlement-operating-display.ts`. No `app/api` caller of `recordLoadPayment`. Invoice generate remains a document. Factoring packet is not cash. 016 did not implement payment. | Cash posting is absent by product decision. | **CERTIFICATION SCOPE CONFLICT (A2).** 014 Scenario G cash portion cannot PASS. | **YES** — 014 will FAIL G cash; that is expected. |
| H | Authoritative operational fact → correct customer consumer (not every screen identical). | `/portals/customer`: DEMO L001 **Delivered / Proof verified / Invoice Ready**. LIVE overlay: **No LIVE Prisma loads currently match these customer shipment IDs.** Overlay copy states DEMO cards stay DEMO. Operator T-102 OOS / HOLD is not forced onto customer Delivered cards. No Dispatch nav on customer portal. | No matching Prisma L001/`sourceRecordId` for overlay. Operator DEMO HOLD vs customer DEMO Delivered remains a dual DEMO story. | **LIVE DATA** (no matching row). **C2** if 014 requires customer to show operator DEMO HOLD. Overlay behavior is **correct** for current LIVE data. | **YES** — 014 can score H against overlay + DEMO cards without treating them as one engine. |
| I | Cross-domain change including Command Center LIVE consumption. | CC LIVE panel: equipment **161**, loads **94**, settlement holds **0** (post-restore), tractor unit present, **Refresh LIVE**. Canonical CC counts remain BOF JSON (L001 HOLD, T-102 OOS, workbook REFERENCE). Copy: operating AUTHORITATIVE counts are DEMO under ADR-009-001. LIVE PATCH/hold does **not** rewrite those DEMO KPIs. | DEMO snapshot still coexists. Driver eligibility not in LIVE panel. | **C2 / ARCHITECTURE:** two Command Center representations, labeled. 015 added LIVE consumption; did not convert DEMO KPIs. | **YES** — 014 can verify LIVE panel after mutation and separately score DEMO KPIs. |
| J | Recovery / resolution of a real exception on the production path. | LIVE proof-hold was created and restored. LIVE equipment OOS cycle restored. LIVE trip-release on `86fd04a8-…` remains Not evaluated / no assignment (load is PLANNED). DEMO T-102 OOS / L001 HOLD were not “resolved” (forbidden LIVE-from-DEMO). | Durable resolution of DEMO OOS/safety still unsupported. LIVE PI-test load has no assignment/release stored. | **C2 + B2** for DEMO/safety recovery. **LIVE DATA** for assignment on the PI-test load. | **YES** — 014 can attempt LIVE recovery on Prisma rows; DEMO recovery remains out of honest LIVE scope. |

## Matrix notes

- Cookie-less: `/loads` `/drivers` `/command-center` **200**; spine/eligibility/proof/equipment mutations **401**; session `null`.
- Test mutations restored: driver ELIGIBLE; equipment AVAILABLE; settlement `e1ee3e8e-…` CREATED; 016 REJECTED proof RECEIVED.
- Prompt 016 did not start Prompt 014 and did not create Prompt 017.
