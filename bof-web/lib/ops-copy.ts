export const OPS_COPY = {
  documentsLead:
    "Fleet-wide credential register across core, primary extension, and secondary workflow files. Every row reads from shared BOF demo data.",
  documentsStory:
    "Use Signal to explain what is blocking action, expired, missing, at risk, or clean during an executive walkthrough.",
  loadsLead:
    "Dispatch command view with live load status, POD and seal posture, and direct links into driver and load detail.",
};
