/** Persisted demo dataset (drivers, documents, overrides). */
export const BOF_DEMO_DATA_STORAGE_KEY = "bof-demo-data-v2";

/** Legacy key — hydrated once, migrated into v2, then removed. */
export const BOF_DEMO_DATA_LEGACY_STORAGE_KEY = "bof-demo-data-v1";
